<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.image.*?>
<?import javafx.scene.layout.*?>

<AnchorPane xmlns="http://javafx.com/javafx"
            xmlns:fx="http://javafx.com/fxml"
            fx:controller="com.cinema.controller.DetailController"
            prefWidth="900"
            prefHeight="600"
            style="-fx-background-color: #020617;">

    <HBox spacing="30"
          alignment="CENTER_LEFT"
          AnchorPane.topAnchor="30"
          AnchorPane.leftAnchor="30"
          AnchorPane.rightAnchor="30"
          AnchorPane.bottomAnchor="30">

        <!-- FOTO FILM -->
        <ImageView fx:id="filmImage"
                   fitWidth="300"
                   fitHeight="580"
                   preserveRatio="true"/>

        <!-- DETAIL FILM -->
        <VBox spacing="15">

            <Label fx:id="titleLabel"
                   text="Judul Film"
                   style="-fx-text-fill: white;
                          -fx-font-size: 26px;
                          -fx-font-weight: bold;"/>

            <Label text="🕒 Jam Tayang"
                   style="-fx-text-fill: #cbd5f5;
                          -fx-font-size: 16px;"/>

            <HBox spacing="10">
                <Button text="13.00" styleClass="time-button" onAction="#openSeat"/>
                <Button text="16.30" styleClass="time-button" onAction="#openSeat"/>
                <Button text="19.00" styleClass="time-button" onAction="#openSeat"/>
            </HBox>

            <Button text="⬅ Kembali"
                    onAction="#backToDashboard"
                    style="-fx-background-color: #1e293b;
                           -fx-text-fill: white;
                           -fx-background-radius: 8;
                           -fx-padding: 8 20;"/>

        </VBox>

    </HBox>
</AnchorPane>
